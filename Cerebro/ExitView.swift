import SwiftUI

struct ExitView: View {
    var body: some View {
        VStack(spacing: 20) {
            Text("See you again!")
                .font(.custom("AppleGaramond", size: 50))
        }
        .padding()
    }
}

